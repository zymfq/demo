<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.zym.demo.dao.AreaDao">

    <resultMap id="areaMap" type="area">

        <id property="areaId" column="area_id"></id>
        <result property="areaName" column="area_name"></result>
        <result property="priority" column="priority"></result>
        <result property="createTime" column="create_time"></result>
        <result property="lastEditTime" column="last_edit_time"></result>

    </resultMap>

    <select id="queryArea" resultMap="areaMap">
        select * from tb_area
    </select>

 <!--   order by priority desc <select id="queryAreaById" resultType="com.zym.demo.entity.Area">
        select area_id , area_name , priority , create_time , last_edit_time
          from tb_area where area_id = #{areaId}
    </select>

    <insert id="insetArea" useGeneratedKeys="true" keyProperty="areaId"
            keyColumn="area_id" parameterType="com.zym.demo.entity.Area">
        insert into tb_area(area_name,priority,create_time,last_edit_time)
          values (#{areaName},#{prioriry},#{createTime},#{lastEditTime})
    </insert>

    <update id="updateArea" parameterType="com.zym.demo.entity.Area">
        update tb_area
        <set>
            <if test="areaName != null">area_name = #{areaName},</if>
            <if test="priority != null">priority = #{priority},</if>
            <if test="lastEditTime != null">last_edit_time = #{lastEditTime}</if>
        </set>

        where area_id = #{areaId}
    </update>

    <delete id="deleteArea">
        delete from tb_area where area_id = #{areaId}

    </delete>
-->

</mapper>